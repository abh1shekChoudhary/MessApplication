# ──────────────────────────────────────────────────────────────────────────────
# SUPERSEDED — This file is no longer used.
#
# The canonical docker-compose configuration has moved to the project root:
#   mess/compose.yaml
#
# That file orchestrates all three services (db, server, frontend) together.
# Run from the mess/ directory:
#   docker compose up -d
# ──────────────────────────────────────────────────────────────────────────────
